# Create React App (react-scripts) depende de paquetes que no declara explícitamente.
# shamefully-hoist eleva las dependencias al node_modules raíz (como hace npm),
# evitando errores de "Cannot find module" al compilar.
shamefully-hoist=true
